# Optional: comma-separated list of app aliases to show before the user has triggered any.
# The same goes for groups. These are only display hints; the agent's whitelist.yaml
# is still the source of truth for what's actually allowed.
VITE_DEFAULT_APPS=chrome,vscode,spotify,steam,discord,notepad
VITE_DEFAULT_GROUPS=work_morning,media_mode
